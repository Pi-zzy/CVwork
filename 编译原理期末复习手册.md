# 编译原理期末通关复习手册

> 教材：陈火旺《程序设计语言编译原理》（第三版）
> 参考：南京大学谭添老师 2026 春季编译原理课件（龙书第 2 版）
> 复习范围（老师划定）：**1.1–1.3，2.3，3.1–3.3，4.1–4.5，5.1–5.2，7.1，10.1–10.3**
> 适用：期末考前 1–2 周冲刺，从上往下顺序阅读即可

---

## 阅读地图（先看这一段，心里有底）

整个编译原理就在干一件事：**把人写的程序，翻译成机器能跑的程序**。这条流水线被切成 6 个阶段：

```
源程序  →  词法分析  →  语法分析  →  语义分析  →  中间代码生成  →  代码优化  →  目标代码生成  →  目标程序
   (字符流)  (单词流)   (语法树)    (带类型的树)   (三地址码)      (优化后的三地址码)   (汇编/机器码)
```

每个阶段都是把"上一份产物"翻译成"更接近机器的产物"。考试基本不会脱离这条主线，所以**遇到任何一个题，先问自己：这是流水线的哪一站？**

复习重点对应这条流水线：
- **第 1 章** = 总览（这条流水线长什么样）
- **第 2 章 2.3** = 工具：用什么语言描述程序（上下文无关文法 CFG）
- **第 3 章** = 站①：词法分析（正则式 → NFA → DFA）
- **第 4 章** = 站②自顶向下：LL(1)、递归下降、预测分析
- **第 5 章** = 站②自底向上：算符优先（重点）、移进-归约
- **第 7 章 7.1** = 站④：中间语言（四元式、三元式、间接三元式、逆波兰式）
- **第 10 章** = 站⑤：代码优化（基本块、DAG、循环优化）

下面按这个顺序逐个吃透。

---

# 第 1 章 引论

## 1.1 什么叫编译程序

**定义**：把一种语言（**源语言**，如 C、Pascal）写的程序翻译成另一种语言（**目标语言**，通常是汇编或机器码）写的等价程序的程序，就叫**编译程序**。

考点小辨析：
- **编译程序 vs 解释程序**：编译程序产生一个等价的目标程序，目标程序自己能跑；解释程序不产生目标程序，它"一边翻译一边执行"。Python、JavaScript 是解释型，C、Rust 是编译型，Java 介于两者之间（先编译成字节码再解释/JIT 执行）。
- **诊断编译程序、优化编译程序、交叉编译程序、可变目标编译程序** 这些名词稍微眼熟一下即可。

## 1.2 编译过程描述

把"输入源程序到输出目标程序"这条流水线切成 6 个阶段（也有的教材切成 5 个，本质一样）：

| 阶段 | 输入 | 输出 | 干的事 |
|------|------|------|--------|
| ① 词法分析 | 字符流 | 单词符号流（Token） | 把字符串切成一个个"单词"并归类 |
| ② 语法分析 | 单词符号流 | 语法树 | 判断单词序列是否符合文法，建立语法结构 |
| ③ 语义分析 + 中间代码生成 | 语法树 | 中间代码（如四元式） | 类型检查、把树翻译成与机器无关的指令 |
| ④ 代码优化 | 中间代码 | 优化后的中间代码 | 等价变换让代码更快/更短 |
| ⑤ 目标代码生成 | 中间代码 | 目标程序（汇编/机器码） | 寄存器分配、指令选择 |

在整条流水线两侧还各有一根"立柱"：
- **符号表管理**：贯穿始终，记录每个标识符的名字、类型、作用域、地址。
- **错误处理**：一发现错误就报告，并尽量恢复继续往下扫，让一次编译能尽量多地发现错误。

**记忆口诀**："词法语法语义中，优化生成不能丢，符号表管贯始终，错误处理两边走。"

## 1.3 编译程序的结构

把上面 5 个阶段配上两根立柱，画在一张图里就是教材开篇那张经典框图。考试要会**画图 + 简述每个阶段**。背的时候按上面的表格背即可。

考点延伸：
- **遍（pass）**：编译程序对源程序或其中间形式"从头到尾扫一次"叫做一遍。多遍 vs 一遍是工程权衡：多遍占内存小、易写；一遍速度快。
- **前端 vs 后端**：词法、语法、语义 + 中间代码生成属于"前端"（与源语言相关，与机器无关）；优化和目标代码生成属于"后端"（与机器相关）。现代编译器（如 LLVM）就是把前端后端切开，IR 作为中间桥梁，所以一个 LLVM IR 能配 N 个前端 × M 个后端。

---

# 第 2 章 2.3 程序语言的语法描述

第 2 章除了 2.3 都不要求，但 2.3 是后面所有章的"语言基础"，必须吃透。

## 2.3.1 字母表、符号串、语言

- **字母表 Σ**：一个有穷的符号集合，如 {0,1}、ASCII、Unicode。
- **符号串**：字母表上符号的有穷序列。空串记作 ε，长度为 0。
- **运算**：
  - 连接：`xy`（x="dog", y="house" → xy="doghouse"）
  - 幂：`s⁰ = ε，sⁱ = sⁱ⁻¹·s`
  - 集合层面：`L · M`（连接）、`L ∪ M`（并）、`L*`（闭包，含 ε 的任意次幂的并）、`L⁺`（正闭包，不含 ε）
- **语言**：字母表上**串的可数集合**。

## 2.3.2 文法 G = (Vₙ, Vₜ, P, S)

**上下文无关文法（CFG）** 是描述程序语言语法最常用的工具，由四元组定义：

- **Vₙ**：非终结符号集（用大写字母如 E、T、F 表示，对应于"语法成分"）
- **Vₜ**：终结符号集（用小写字母、运算符等表示，对应于"词法单元"）
- **P**：产生式集，形如 A → α，A ∈ Vₙ，α ∈ (Vₙ ∪ Vₜ)\*
- **S**：开始符号，S ∈ Vₙ

简写形式：`A → α | β | γ` 等价于 `A → α, A → β, A → γ`。注意 `|` 是元符号（描述文法的符号），不属于文法本身。

## 2.3.3 推导、归约、句型、句子

- **直接推导 ⇒**：αAβ ⇒ αγβ（A → γ 是一条产生式）
- **多步推导 ⇒\***（0 步及以上）、**⇒⁺**（1 步及以上）
- **最左推导 / 最右推导**：每一步都替换最左/最右的非终结符。最右推导也叫**规范推导**。
- **句型**：从开始符号 S 出发可推出的任意串（可含非终结符）
- **句子**：只含终结符的句型
- **语言**：L(G) = { w | S ⇒\* w，w ∈ Vₜ\* } 所有句子的集合

**归约**就是推导的反过程：把产生式右部替换成左部。自底向上语法分析就是不停归约。

## 2.3.4 语法分析树 & 二义性

- **语法分析树**：推导过程的树形表示。根是 S，叶子从左到右拼起来是句型；每个内部节点对应一次产生式应用。
- **二义性文法**：存在某个句子有 ≥2 棵不同的分析树（或等价地：存在 ≥2 个最左推导/最右推导）。
- 经典二义性例子：`E → E+E | E*E | (E) | id`，句子 `id+id*id` 既能解释为 `(id+id)*id` 又能解释为 `id+(id*id)`。**程序语言不允许二义性**，因为同一句话不能有两种意思。
- **消除二义性的标准技巧**：分层（高优先级运算放更深一层）：
  ```
  E → E + T | T
  T → T * F | F
  F → ( E ) | id
  ```
  这样 `+` 在外、`*` 在内，自动保证"先乘后加 + 左结合"。**这套文法贯穿后面整本书，一定要记住！**

## 2.3.5 文法的分类（Chomsky 谱系）

| 类型 | 名称 | 产生式形式 | 识别它的自动机 |
|------|------|-----------|---------------|
| 0 型 | 短语文法 | α → β（α 含至少一个非终结符） | 图灵机 |
| 1 型 | 上下文有关文法 (CSG) | αAβ → αγβ，γ ≠ ε | 线性界限自动机 |
| **2 型** | **上下文无关文法 (CFG)** | A → γ | **下推自动机 (PDA)** |
| **3 型** | **正则文法 (RG)** | A → aB / A → a | **有穷自动机 (DFA/NFA)** |

考点：能区分一个文法属于哪一类；知道"正则 ⊊ 上下文无关 ⊊ 上下文有关 ⊊ 短语"的包含关系；**正则语言无法描述 aⁿbⁿ 这类配对结构（要用上下文无关）**。

---

# 第 3 章 词法分析

任务：从字符流中识别**单词符号**（Token）。

## 3.1 对于词法分析器的要求

### 3.1.1 单词符号种类（5 类，一定要记）

1. **关键字（保留字）**：begin、end、if、while……
2. **标识符**：变量名、过程名等
3. **常数**：整型、浮型、布尔型、字符串……
4. **运算符**：+、-、\*、/、:=、<= ……
5. **界符**：( ) , ; { } ……

输出形式：每个单词输出一个二元组 `(种别码, 属性值)`。
- 种别码是固定的整数（如保留字 1 类、标识符 2 类、整数 3 类、运算符 4 类、界符 5 类，或者一类一码）
- 属性值：对标识符是其在符号表中的入口指针；对常数是常数值；对保留字/运算符/界符通常为空。

> ⚠️ 你已经在**实验一**里实现过这一套了，那里采用"一类一码"方案（25 个种别码），考试时随便采用哪种方案都行，能自圆其说即可。

### 3.1.2 词法分析器作为子程序

- 与语法分析器的接口：**作为语法分析器的子程序被调用**。语法分析器每要一个 token，就调一次 `getsym()`，词法分析器扫到下一个 token 就返回。
- **超前搜索**：识别复合运算符（`:=`、`<=`、`<>`、`>=`）和某些保留字时必须向前多看一位。看完发现"不要"，要回退一位。
- 不要做太多事：错误恢复、宏展开等通常放在预处理或语法层。

## 3.2 词法分析器的设计（状态转换图法）

### 3.2.1 状态转换图

四要素：
- **结点（状态）**：词素识别过程中可能出现的情况，可看作"已扫描部分的总结"
- **边（转换）**：标号是一个或多个字符；当前状态 s，遇到字符 a，就走标号为 a 的边
- **初态**：用 Start 边或双圈外的箭头标出
- **终态（接受状态）**：双圈表示。标 `*` 表示"最后读入的字符不属于该词素，需回退一位"

### 3.2.2 设计步骤（考试常考的"画图题"）

1. 列出所有需要识别的模式（保留字、标识符、整数、各种运算符……）
2. 为每种模式画一个状态转换图
3. **把所有图合并**：引入一个公共初态，对每个模式画一条对应的分支
4. 对保留字和标识符往往共用一段图：先按"标识符规则"扫，扫完后查保留字表，若是保留字就归到保留字种别

### 3.2.3 从状态转换图到代码

最直接的实现：用 `switch (state)` 或 `goto` 模拟状态跳转：

```c
state = 0;
while (true) {
    c = nextChar();
    switch (state) {
      case 0: if (isLetter(c)) state = 1;
              else if (isDigit(c)) state = 2;
              else if (c == ':') state = 3;
              ... ;
              break;
      case 1: if (isLetter(c) || isDigit(c)) state = 1;
              else { retract(); return lookupKeyword(); }
              break;
      ...
    }
}
```

> 你的**实验一 lexer.py** 用的就是这套思路，不过用 Python 把 switch 拆成多个 `_read_xxx` 方法。

## 3.3 正规表达式与有限自动机（重头戏！）

### 3.3.1 正规式（Regex）

定义（递归）：
- ε 是正规式，L(ε) = {ε}
- a ∈ Σ 是正规式，L(a) = {a}
- 若 r、s 是正规式，则：
  - `r | s` 是正规式，L(r|s) = L(r) ∪ L(s)（**选择**）
  - `rs` 是正规式，L(rs) = L(r) · L(s)（**连接**）
  - `r*` 是正规式，L(r\*) = (L(r))\*（**闭包**）
- 优先级：`*` > 连接 > `|`

**典型正规式**：
- 标识符：`letter (letter | digit)*`
- 无符号整数：`digit digit*` 或 `digit⁺`
- 实数：`digit⁺ (. digit⁺)? (E [+−]? digit⁺)?`
- C 风格注释：`/\* (任何字符)* \*/`

### 3.3.2 有限自动机（DFA / NFA）

**DFA（确定的有穷自动机）M = (S, Σ, δ, s₀, F)**：
- S：状态集
- Σ：输入字母表
- δ：S × Σ → S，**每个 (状态, 字符) 至多有一条出边**
- s₀：初态
- F：终态集

**NFA（不确定的有穷自动机）**：放松两条约束——
- 同一个 (状态, 字符) 可以有多条出边；
- 允许 ε 边（不消耗输入就能转移）。

**核心定理**：DFA、NFA、正规式三者等价（描述同一类语言：**正则语言**）。

### 3.3.3 正规式 → NFA（Thompson 构造法）

按正规式的递归结构构造（每条规则给出一个有"唯一初态、唯一终态"的 NFA 片段）：

```
ε:        ─ε→○                       (一个 ε 边)

a:        ─a→○                       (一条 a 边)

r | s:    ─ε→ N(r) ─ε→○             (并联两个分支)
            ─ε→ N(s) ─ε→
            
r·s:      N(r) ─ε→ N(s)              (串联)

r*:       ─ε→ N(r) ─ε→○
                ↑___ε___|
            ─ε→________→               (旁路 + 回路)
```

每应用一个运算符，最多多 2 个状态。所以 NFA 的状态数线性于正规式的长度。

**例**：`(a|b)*abb` 的 NFA（教材经典例题）

### 3.3.4 NFA → DFA（子集构造法）

核心思想：**DFA 的每个状态对应 NFA 的一个状态子集**——"NFA 在读入若干字符后，可能同时处在的所有状态"组成的集合。

两个基本运算：
- **ε-closure(T)**：从 T 中各状态出发，只走 ε 边能到达的所有状态的集合（含 T 自身）
- **move(T, a)**：从 T 中各状态出发，沿标号为 a 的边能直接到达的状态的集合

算法（背诵级）：
```
1. 初始状态：A = ε-closure({s₀})，加入 Dstates，标"未处理"
2. while (Dstates 中有未处理状态 T) {
       标记 T 为已处理
       for (每个输入符号 a) {
           U = ε-closure(move(T, a))
           if U 不在 Dstates 中：加入并标"未处理"
           Dtran[T, a] = U
       }
   }
3. 凡是子集中含 NFA 终态的，都是 DFA 终态。
```

**例题套路**：给一个 NFA → 列 ε-closure → 列每个状态对每个字符的转移 → 列出转换表 → 画 DFA 图。

### 3.3.5 DFA 状态最小化（Hopcroft 算法）

目标：合并"等价"的状态，得到状态数最少的 DFA。

**两状态等价**：对任何输入串 w，从这两个状态出发，要么都进终态，要么都不进。

算法（划分法）：
```
1. 初始划分 Π = { F, S − F }（终态组 + 非终态组）
2. 反复细分：对每个组 G，看 G 中每个状态在每个输入字符下落入 Π 的哪个组。
   若不一致就把 G 拆开。
3. 直到 Π 不再变化。
4. 每个组取一个代表，作为最小 DFA 的状态；转移按代表之间的关系建立。
```

直观理解：能把状态区分开的"证据"就是某个输入串。空串就能区分终态和非终态；继续找一字符串区分非终态组里的状态……

### 3.3.6 一条龙总结

```
正规式 ──Thompson──→ NFA ──子集构造──→ DFA ──最小化──→ 最小 DFA ──编程──→ 词法分析器
```

这一串 **必考**。建议自己手算一遍 `(a|b)*abb` 走完全程。

---

# 第 4 章 自上而下语法分析

任务：判断单词符号流是否符合文法，并构造分析树。

> 本章共有 5 个考点小节，按"4.1 → 4.2 → 4.3 → 4.4 → 4.5"顺序学。

## 4.1 语法分析器的功能

- **输入**：词法分析器送来的单词序列
- **输出**：要么报告"通过 + 语法树"，要么报告"语法错误及位置"
- 两大类技术：
  - **自顶向下 (Top-down)**：从开始符号 S 出发，最左推导，尝试推出输入串。代表：递归下降、LL(1) 预测分析。
  - **自底向上 (Bottom-up)**：从输入串出发，不停归约，直到归约成 S。代表：算符优先、LR 系列。

## 4.2 自上而下分析面临的问题

自顶向下的核心难题：**遇到 A → α | β | γ 时，不知道该选哪条产生式**。

它会带来三大问题：

### 4.2.1 回溯（Backtracking）

最朴素的做法是"挨个试"，错了就退回去重试——但这样效率极低（指数级），而且语义动作很难撤销。**实际编译器要避免回溯**。

### 4.2.2 左递归（Left Recursion）

**直接左递归**：文法中存在 `A → Aα` 形式的产生式。

为什么是死穴？递归下降时，函数 `parseA()` 会立刻调 `parseA()`，**无限递归直至栈溢出**。

**消除直接左递归**：

```
原文法:   A → Aα | β              (β 不以 A 开头)
新文法:   A  → β A'
         A' → α A' | ε
```

推广（多个左递归 + 多个非左递归产生式）：
```
原:  A → Aα₁ | Aα₂ | … | Aαₘ | β₁ | β₂ | … | βₙ
新:  A  → β₁ A' | β₂ A' | … | βₙ A'
     A' → α₁ A' | α₂ A' | … | αₘ A' | ε
```

经典例子：表达式文法
```
原:  E → E + T | T
     T → T * F | F
     F → ( E ) | id
新:  E  → T E'
     E' → + T E' | ε
     T  → F T'
     T' → * F T' | ε
     F  → ( E ) | id
```
**这就是教材文法 4.2**，也是你**实验二**用的文法。

**间接左递归**：经过几步推导才出现左递归（如 S→Aa, A→Sd 时 S⇒Aa⇒Sda）。算法：把所有非终结符按某个顺序排好，逐个用前面的非终结符代入展开，把间接的变成直接的，然后再用上面的方法消除。

### 4.2.3 回溯导致的另一个问题：左公共因子

`A → αβ₁ | αβ₂` 时，光看下一个字符 α 没法决定走哪条。**提取左因子**：

```
原:  A → α β₁ | α β₂
新:  A  → α A'
     A' → β₁ | β₂
```

经典案例 if-else：
```
原:  stmt → if E then S else S | if E then S
提:  stmt  → if E then S stmt'
     stmt' → else S | ε
```

## 4.3 LL(1) 分析法

LL(1) 三个字母的含义：
- **第一个 L**：Left to right scanning（从左到右扫输入）
- **第二个 L**：Leftmost derivation（构造最左推导）
- **1**：每步只向前看 1 个 token

### 4.3.1 FIRST 集和 FOLLOW 集（极重要！）

#### FIRST(α)

> "α 能推出的所有串的**首符号**的集合；如果 α 能推出 ε，ε 也在里面。"

计算规则（对单个符号 X，再扩展到串）：
1. X 是终结符 → FIRST(X) = { X }
2. X → ε ∈ P → 把 ε 加入 FIRST(X)
3. X → Y₁Y₂…Yₖ ∈ P：
   - 把 FIRST(Y₁) − {ε} 全部加入 FIRST(X)
   - 若 ε ∈ FIRST(Y₁)，再把 FIRST(Y₂) − {ε} 加入；……
   - 若 ε 在所有 FIRST(Yᵢ) 中，把 ε 加入 FIRST(X)
4. 对串 X₁X₂…Xₙ 用同样的方法（看链上每个 Xᵢ 能不能推 ε）

#### FOLLOW(A)（只对非终结符 A 定义）

> "在所有句型中，可能**紧跟在 A 后面**的终结符的集合。"

计算规则（迭代直至稳定）：
1. 把句尾标志 `#`（或 `$`）加入 FOLLOW(S)（S 为开始符号）
2. 若有 `A → α B β`，把 FIRST(β) − {ε} 加入 FOLLOW(B)
3. 若有 `A → α B`，**或** `A → α B β` 且 ε ∈ FIRST(β)，把 FOLLOW(A) 加入 FOLLOW(B)

#### 经典例题（文法 4.2，必算）

```
E  → T E'
E' → + T E' | ε
T  → F T'
T' → * F T' | ε
F  → ( E ) | id
```

| 非终结符 | FIRST | FOLLOW |
|---------|-------|--------|
| E  | { (, id }    | { ), # }       |
| E' | { +, ε }     | { ), # }       |
| T  | { (, id }    | { +, ), # }    |
| T' | { *, ε }     | { +, ), # }    |
| F  | { (, id }    | { +, \*, ), # } |

> 💡 **快速记忆诀窍**：
> - **FIRST**：把式子右部"读一遍"，把开头能出现的终结符记下来；若开头能推 ε，再往后读一位。
> - **FOLLOW**：找到 A 在哪些产生式的右部出现过，A 后面是谁、谁的 FIRST 就是 FOLLOW(A) 的候选；如果 A 在式子末尾，则把"产生式左部的 FOLLOW"也并进来。

### 4.3.2 LL(1) 文法的判定

文法 G 是 LL(1) 文法，当且仅当对每个产生式 `A → α | β`：

1. FIRST(α) ∩ FIRST(β) = ∅
2. 若 ε ∈ FIRST(β)，则 FIRST(α) ∩ FOLLOW(A) = ∅（α 不能与 A 之后的终结符冲突；β 反之亦然）

满足上述条件就意味着：**根据当前的 1 个输入符号，就能唯一确定该用哪条产生式**。

LL(1) 文法必然是无二义且无左递归的。

### 4.3.3 几个重要结论

- 二义文法 / 左递归文法都不是 LL(1)
- LL(1) ⊊ 上下文无关
- 大多数程序设计语言的文法都能改造成 LL(1)，但不是所有

## 4.4 递归下降分析程序构造

思路：**每个非终结符写一个函数**，函数体根据当前 token 选择某条产生式，递归地调子函数。

模板（对文法 4.2 改造后的版本）：

```python
def E():
    T()
    Eprime()

def Eprime():
    if cur == '+':
        match('+'); T(); Eprime()
    # ε 情形：什么都不做即返回（注意此时 cur 必须在 FOLLOW(E') = {), #} 中）

def T():
    F()
    Tprime()

def Tprime():
    if cur == '*':
        match('*'); F(); Tprime()

def F():
    if cur == '(':
        match('('); E(); match(')')
    elif cur == 'id':
        match('id')
    else:
        error()

def match(expected):
    global cur
    if cur == expected: cur = nextToken()
    else: error()
```

特点：
- ✅ 写起来直观，易于手工编写复杂语言的分析器（gcc、clang 早期都用过）
- ✅ 易于嵌入语义动作和错误恢复
- ❌ 只能处理 LL(1) 文法（需要先消除左递归、提左因子）
- ❌ 大型语言函数嵌套太深可能栈溢出

## 4.5 预测分析程序（表驱动）

把"用哪条产生式"的决定**做成一张表**，再用统一的栈式总控算法解释这张表。

### 4.5.1 预测分析表 M[A, a] 的构造

输入：文法 G；输出：预测分析表 M（行：非终结符；列：终结符 + `#`）

算法：
```
对每条产生式 A → α:
    1. 对 FIRST(α) 中的每个终结符 a：
           M[A, a] = "A → α"
    2. 若 ε ∈ FIRST(α)：
           对 FOLLOW(A) 中的每个终结符 b（包括 #）：
               M[A, b] = "A → α"
所有未填的格子置为 error。
```

**文法 4.2 的预测分析表（教材表 4.1）**——你**实验二**就用的这张表：

|     | i        | +         | *         | (        | )        | #        |
|-----|----------|-----------|-----------|----------|----------|----------|
| E   | E→TE'    |           |           | E→TE'    |          |          |
| E'  |          | E'→+TE'   |           |          | E'→ε     | E'→ε     |
| T   | T→FT'    |           |           | T→FT'    |          |          |
| T'  |          | T'→ε      | T'→*FT'   |          | T'→ε     | T'→ε     |
| F   | F→i      |           |           | F→(E)    |          |          |

判断 LL(1) 的另一种说法：**M 中每格至多放一条产生式**（无冲突）。

### 4.5.2 总控算法（栈式 + 表驱动）

```
栈初始化: [#, S]      （栈底为 #，开始符号 S 在顶）
输入串末尾追加 #

repeat:
    X = 栈顶, a = 当前输入符号
    if X == a == '#': accept, return True
    elif X == a:                    # 终结符匹配
        弹栈; 输入指针前移
    elif X 是终结符 但 X != a: error, return False
    elif M[X, a] 为空:               # 查表失败
        error, return False
    elif M[X, a] = "X → Y₁Y₂…Yₖ":
        弹栈; 把 Yₖ, Yₖ₋₁, …, Y₁ 依次压栈（即倒序压栈）
        (若产生式为 X → ε, 只弹栈不压任何东西)
```

> 你的**实验二 predictor.py** 就是这个算法的逐字翻译。

### 4.5.3 经典例题：分析 `i + i * i #`

逐步骤的栈、输入、动作如下（节选）：

| 步 | 栈 | 剩余输入 | 动作 |
|----|----|---------|------|
| 1 | #E      | i+i*i# | E→TE' |
| 2 | #E'T    | i+i*i# | T→FT' |
| 3 | #E'T'F  | i+i*i# | F→i  |
| 4 | #E'T'i  | i+i*i# | 匹配 i |
| 5 | #E'T'   | +i*i#  | T'→ε |
| 6 | #E'     | +i*i#  | E'→+TE' |
| 7 | #E'T+   | +i*i#  | 匹配 + |
| 8 | #E'T    | i*i#   | T→FT' |
| … | …       | …      | … |
| n | #       | #      | accept |

考试时建议**手工画完整的表**，节省时间。

---

# 第 5 章 自下而上语法分析

任务：从输入串出发，不停归约，直到归约到开始符号 S。

## 5.1 自下而上分析基本问题

### 5.1.1 移进-归约的工作过程

用一个**栈**保存"已扫描或已归约出来的符号"，剩下的输入串等待处理。每一步只做两件事：

- **移进 (shift)**：把下一个输入符号压栈
- **归约 (reduce)**：把栈顶若干符号（形如某产生式的右部）替换成产生式左部

接受条件：栈中只剩 `#S`，输入只剩 `#`。

**例**：用文法 `E → E+E | E*E | (E) | id` 分析 `id*id`：

| 栈 | 输入 | 动作 |
|----|------|------|
| #          | id*id# | 移进 |
| #id        | *id#   | 归约 E→id |
| #E         | *id#   | 移进 |
| #E*        | id#    | 移进 |
| #E*id      | #      | 归约 E→id |
| #E*E       | #      | 归约 E→E*E |
| #E         | #      | accept |

### 5.1.2 关键概念：句柄（Handle）

> **句柄**：最右句型的一个最左完整产生式右部，对它进行归约对应着最右推导的最后一步。

直白说：**句柄就是"该被归约"的那一段**。每次移进-归约的核心问题就是：**何时归约？归约到哪个非终结符？**

句柄有几条性质：
- 句柄出现时，**它在栈顶**
- 无二义文法的每个句型有且只有一个句柄
- 把句柄归约一次，就回到上一步最右推导的句型

### 5.1.3 移进-归约冲突（Shift-Reduce Conflict）

如果在某个时刻，既能归约又能移进，且分析器**说不清楚该做哪个**，就出现冲突。典型例子是悬空 else：

```
S → if E then S else S | if E then S | other
```

栈顶是 `if E then S`，下一个输入是 `else`：是该把 `if E then S` 归约（按第二条），还是该移进 `else`（按第一条）？常见约定是**移进**——`else` 与最近的 `if` 配对。

## 5.2 算符优先分析（教材重点）

这是一种"以算符为中心"的自底向上技术，特别适合**算术表达式**。

### 5.2.1 算符文法（OG）和算符优先文法（OPG）

**算符文法 (Operator Grammar, OG)**：任何产生式的右部**不含两个相邻的非终结符**。即不存在 `A → …BC…`（B、C 都是非终结符且相邻）。

**算符优先文法 (Operator Precedence Grammar, OPG)**：是 OG，且对任意两个终结符 a、b，最多只有 `<·`、`=·`、`·>` 三种关系中的一种。

文法 5.4（教材 P90 例 5.4，**实验三**用的就是它）：
```
E → E + T | T
T → T * F | F
F → ( E ) | id
```
这是一个 OPG。

### 5.2.2 三种算符优先关系

对终结符 a、b：
- `a <· b`：a 的优先级**低于** b（应先归约 b 那一侧）
- `a =· b`：a 与 b 优先级相等（在同一组里，如 `(` 与 `)`）
- `a ·> b`：a 的优先级**高于** b（应先归约 a 那一侧）

**注意**：三种关系不对称！`a <· b` 不等于 `b ·> a`，可能两边都没关系。

### 5.2.3 构造优先关系表（机械步骤）

**Step 1**：算 **FIRSTVT** 和 **LASTVT**。

> **FIRSTVT(P)**：P 能推出的所有串的**首终结符**（注意是"终结符"而不是首符号）
> **LASTVT(P)**：P 能推出的所有串的**末终结符**

计算规则：
- 若 P → a… 或 P → Qa…，则 a ∈ FIRSTVT(P)
- 若 P → Q… 且 a ∈ FIRSTVT(Q)，则 a ∈ FIRSTVT(P)

LASTVT 对称（看末尾）。

对例 5.4 算：
| 非终结符 | FIRSTVT | LASTVT |
|---------|---------|--------|
| E | { +, \*, (, id } | { +, \*, ), id } |
| T | { \*, (, id }     | { \*, ), id }    |
| F | { (, id }         | { ), id }        |

**Step 2**：扫每条产生式右部 X₁X₂…Xₙ：

- 若 Xᵢ、Xᵢ₊₁ 都是终结符 → `Xᵢ =· Xᵢ₊₁`
- 若 Xᵢ 终结符、Xᵢ₊₁ 非终结符 → 对每个 b ∈ FIRSTVT(Xᵢ₊₁)：`Xᵢ <· b`
- 若 Xᵢ 非终结符、Xᵢ₊₁ 终结符 → 对每个 a ∈ LASTVT(Xᵢ)：`a ·> Xᵢ₊₁`
- 若 Xᵢ、Xᵢ₊₂ 都是终结符、Xᵢ₊₁ 非终结符 → `Xᵢ =· Xᵢ₊₂`

**Step 3**：处理输入串两端的 `#`（相当于增加产生式 S' → #E#）：
- `#` 与 FIRSTVT(E) 中每个 b：`# <· b`
- LASTVT(E) 中每个 a 与 `#`：`a ·> #`
- `# =· #`（结束标志）

最终得到的优先表（**教材 P90 表 5.1**，**实验三**生成出来的就是它）：

|     | + | \* | ( | ) | id | # |
|-----|---|----|---|---|----|---|
| +   | ·>| <· | <·| ·>| <· | ·>|
| \*  | ·>| ·> | <·| ·>| <· | ·>|
| (   | <·| <· | <·| =·| <· |   |
| )   | ·>| ·> |   | ·>|    | ·>|
| id  | ·>| ·> |   | ·>|    | ·>|
| #   | <·| <· | <·|   | <· | =·|

### 5.2.4 算符优先分析的总控算法

```
栈 S = [ # ]，输入末尾加 #，指针 ip 指向首字符。

repeat:
    取栈顶最靠近顶部的终结符 a，当前输入符号 b
    if a == b == #: accept
    elif a <· b 或 a =· b:
        b 入栈; ip ++
    elif a ·> b:
        # 寻找最左素短语并归约
        从栈顶向下找：第一对 (Q, R) 满足 Q <· R (R 在 Q 之后)
        把 [R..顶] 这一段当作素短语，归约为 N
    else:
        error
```

### 5.2.5 最左素短语（Leftmost Prime Phrase）

> **素短语**：句型的语法树中，包含至少一个终结符并且不再有更小素短语的短语。
> **最左素短语**：最左边的那个素短语。

直白说：算符优先分析每次归约的就是"栈中最早出现 `<·` 到下一个 `·>` 之间的那一段"。

例如分析 `id+id*id`：
- 一开始全是 `id < + < * < id`
- 推进到第一个 `·>`：`id ·> *` → 归约 `id` 为 N
- 继续……

### 5.2.6 算符优先 vs LR

**优点**：
- 表小、构造简单、适合手写
- 跳过单非产生式（`E → T`、`T → F` 这种），效率高

**缺点**：
- 适用文法窄（必须是 OPG）
- 跳过了一些归约动作，**不构造完整的语法树**（不能直接用于语义分析）
- 优先关系可能丢失结构信息（错误处理较弱）

LR 系列（SLR、LR(1)、LALR(1)）才是工业界主流。陈火旺教材对 LR 讲得较浅，老师标的复习重点也没把 LR 列进去，所以**期末重点是算符优先**。但概念性知识还是要懂：
- LR(k) = Left-to-right + Rightmost derivation (反向) + 向前看 k 个
- SLR：用 FOLLOW 集决定归约时机
- LR(1)：把向前看符号也放进项里
- LALR(1)：把 LR(1) 中"同心"的项集合并，状态数降到与 SLR 相同

---

# 第 7 章 中间代码生成

## 7.1 中间语言

为什么要中间语言？
- 把"前端（与源语言相关）"和"后端（与机器相关）"解耦
- 便于做与机器无关的优化
- 便于一个前端配多个后端（多机器移植）

教材重点讨论 4 种中间表示：

### 7.1.1 逆波兰表示（后缀式）

> 把运算符放在运算分量**之后**。

| 中缀          | 后缀         |
|---------------|-------------|
| a + b         | a b +       |
| a + b * c     | a b c \* +  |
| (a + b) * c   | a b + c \*  |
| − a           | a @  （@ 表示一元减）|
| a := b + c    | a b c + :=  |

**特点**：括号消失，求值用栈即可：扫一遍，遇运算分量入栈，遇运算符弹出对应数量分量算完再压栈。

### 7.1.2 三元式

格式：`(op, arg1, arg2)`，每条三元式有唯一编号。引用前面的三元式时用编号代替"中间结果变量"。

例：`a := b * −c + b * −c` 翻译成：

| 编号 | op  | arg1 | arg2 |
|------|-----|------|------|
| (1)  | @   | c    |      |  ← 一元减
| (2)  | \*  | b    | (1)  |
| (3)  | @   | c    |      |
| (4)  | \*  | b    | (3)  |
| (5)  | +   | (2)  | (4)  |
| (6)  | :=  | (5)  | a    |

**特点**：不引入临时变量，但**插入/删除/重排三元式**时编号要全部重算，不利于优化。

### 7.1.3 间接三元式

在三元式之上再加一张**间接码表**，里面按执行顺序存"指向三元式表的指针/编号"。要重排时只动间接表，不动三元式表。这是对三元式的改良。

### 7.1.4 四元式（最常用！）

格式：`(op, arg1, arg2, result)`。

例：`a := b * −c + b * −c` 翻译成：

| 序号 | op    | arg1 | arg2 | result |
|-----|-------|------|------|--------|
| (1) | uminus| c    | _    | t1     |
| (2) | \*    | b    | t1   | t2     |
| (3) | uminus| c    | _    | t3     |
| (4) | \*    | b    | t3   | t4     |
| (5) | +     | t2   | t4   | t5     |
| (6) | :=    | t5   | _    | a      |

**约定**：
- 单目运算 arg2 留空
- 无条件转移：`(j, _, _, L)` 表示 `goto L`
- 条件转移：`(j<, arg1, arg2, L)` 表示 `if arg1 < arg2 goto L`
- 函数调用：`(param, x, _, _)` 设参数；`(call, f, n, result)` 调用 f 用 n 个参数

**特点**：
- 含 result 字段（每条四元式给临时变量命名），重排/删除时不影响其他四元式 → **最有利于优化**
- 是绝大多数教科书和工业编译器（如 GCC 的 RTL、LLVM IR 思想类似）采用的中间形式
- 与三地址码本质等价：`x := y op z` 就是 `(op, y, z, x)`

### 7.1.5 三地址码（南大课件 + 龙书叫法）

```
x := y op z         普通运算
x := op y           一元运算
x := y              复制
goto L              无条件跳
if x relop y goto L 条件跳
param x; call p, n  过程调用
x := y[i]; x[i] := y 数组读写
x := &y; x := *y    取地址、间接
```

**SSA（静态单赋值）**：是三地址码的一种特殊形式，每个变量只赋值一次。汇合处用 φ 函数。SSA 让数据流分析非常清晰，**LLVM IR 就基于 SSA**。考试不必深究，知道是优化的好朋友即可。

### 7.1.6 各种中间形式对比

| 形式 | 是否含 result | 优化友好度 | 占用空间 |
|------|--------------|-----------|---------|
| 后缀式 | 无           | 差        | 小      |
| 三元式 | 隐含（用编号）| 较差      | 中      |
| 间接三元式 | 隐含     | 良        | 略大    |
| 四元式 | 有显式临时变量 | **最好** | 大      |

---

# 第 10 章 代码优化

## 10.1 概述

**优化**：在保持程序语义不变的前提下，让生成的代码运行更快/更小/更省能耗。

### 10.1.1 优化的分类

按作用范围：
- **局部优化**：只在一个**基本块**内做（不跨越分支、跳转）
- **循环优化**：以循环为单位
- **全局优化**：跨基本块乃至整个过程
- **过程间优化**：跨函数

按机器相关性：
- **机器无关优化**：在中间代码上做（本章重点）
- **机器相关优化**：在目标代码上做（如寄存器分配、指令调度）

### 10.1.2 常见优化类型（不局限于局部）

| 优化 | 例子 |
|------|------|
| 公共子表达式消除 (CSE) | 两次出现的 `a*b` 只算一次 |
| 复制传播 (Copy Propagation) | `x := y; ... z := x + 1` → `z := y + 1` |
| 常量合并/折叠 (Constant Folding) | `x := 2 * 3` → `x := 6` |
| 死代码删除 (Dead Code Elim) | 删除"赋了之后再没被用"的赋值 |
| 强度削弱 (Strength Reduction) | 把 `x*2` 换成 `x<<1`；循环中 `i*4` 换成累加 |
| 代码外提 (Loop-invariant Code Motion) | 循环中不变的 `t := limit - 2` 移出循环 |
| 归纳变量删除 | 多个步调一致的归纳变量合并 |

## 10.2 局部优化（基本块 + DAG）

### 10.2.1 基本块的划分（必考！）

> **基本块**：一段连续的中间代码，只能从第一条进入，只能从最后一条离开（中间不分叉、不跳出/跳入）。

**算法**：
1. 找出所有"入口语句"：
   - 第一条语句
   - 任何跳转语句的目标
   - 紧跟在条件跳转或无条件跳转之后的语句
2. 每个入口语句到下一个入口语句之前（或到程序末尾），都是一个基本块。

**例题**：给一段三地址代码（10 条左右），先找入口语句、再切基本块。

### 10.2.2 基本块的 DAG 表示（很常考）

把基本块画成一个 DAG，每个节点表示一次**值的计算**，节点上挂着所有"持有这个值"的变量名。

构造规则：
1. 对每个初值变量建一个叶节点
2. 对每条语句 `x := y op z`：
   - 找/建一个 `(op, node(y), node(z))` 的中间节点 N
   - 把 x 加入 N 的附加标签，并把 x 从原节点的标签中移除
3. 对复制语句 `x := y`：让 x 直接挂到 y 所在节点的标签上

**DAG 能一举做四件事**：

| 优化 | 操作 |
|------|------|
| 公共子表达式消除 | 同样的 (op, 左子, 右子) 只建一次节点 |
| 复制传播       | 复制语句直接合并标签 |
| 死代码删除     | 出口处不活跃的变量名，从 DAG 重建代码时不输出 |
| 常量合并       | 若两个孩子都是常量节点，直接算出新常量节点 |

**例**（教材经典）：

```
原代码:           DAG:
(1) t1 := 4 * i        节点 N1 = (*, 4, i), 标签 {t1}
(2) t2 := a[t1]        N2 = (=[], a, N1),  {t2}
(3) t3 := 4 * j        N3 = (*, 4, j),    {t3}
(4) t4 := a[t3]        N4 = (=[], a, N3), {t4}
(5) t5 := t2 + t4      N5 = (+, N2, N4),  {t5}
(6) a[t1] := t5        a 的"新值" = ([]= 节点)
(7) t6 := 4 * i        — 复用 N1，{t1, t6}
```

第 (7) 行因为 `4*i` 已经算过且 i 没变 → 直接合并到 N1，省一次乘法。

### 10.2.3 由 DAG 重建优化后的基本块

按一定顺序遍历 DAG 节点，为每个节点输出一条赋值（变量名取标签中的某个；若该节点的所有标签变量在出口处都不活跃，直接删除）。这步把上述 4 种优化"一次性输出"。

## 10.3 循环优化（重头戏）

> 程序大部分时间都花在循环里 → 优化循环最划算。

### 10.3.1 必要前置：流图、必经结点、回边、自然循环

**流图（控制流图 CFG）**：
- 节点 = 基本块；边 = 控制流可能性
- 入口节点 = 第一个基本块；出口节点 = 程序结束的块

**必经结点（Dominator）**：从入口到 n 的**所有**路径都经过 d，则 d 必经 n，记作 `d dom n`。
- `dom` 是偏序关系；每个节点的所有必经结点构成一棵**必经结点树**

**回边**：边 `a → b`，且 `b dom a`。

**自然循环（Natural Loop）**：给定回边 `n → d`，由 d 加上"不经过 d 就能到达 n"的所有节点组成的集合，称为该回边对应的自然循环。`d` 叫**循环头**。

性质：
- 自然循环有唯一入口（即循环头）
- 自然循环内每个节点都有路径回到入口

### 10.3.2 循环优化的三大手段

#### 手段一：代码外提（Loop-invariant Code Motion）

把"循环不变"的运算搬到循环外。

**判断不变运算**：A 是某条三地址语句 `s: x := y op z` 的右部，如果在循环内：
- y、z 都是常量，**或**
- y、z 的所有定值（reaching definition）都在循环外，**或**
- y、z 在循环中只被一条不变运算定值

那 s 就是不变运算。

**外提的条件**：
- s 所在的块必须是循环所有出口的必经结点（否则可能改变语义）
- x 在循环中只在 s 处被赋值
- x 在 s 之前不被使用（除了 s 自己的右部）

外提时插入到循环头的**前置块（preheader）**——这是个新建的、专门容纳外提代码的块。

#### 手段二：强度削弱（Strength Reduction）

用便宜的运算替换贵的运算。最经典：循环中**乘法 → 加法**。

```c
原:                          强度削弱后:
for (i=1; i<=10; i++)         t = 4;        // 初始
    A[4*i] = ...              for (i=1; i<=10; i++) {
                                  A[t] = ...
                                  t = t + 4;
                              }
```

`4*i` 是"基本归纳变量 i"派生出来的"归纳变量"。

#### 手段三：归纳变量删除

> **基本归纳变量 (Basic Induction Variable, BIV)**：循环中只被形如 `i := i ± c`（c 是常量或循环不变量）的语句赋值的变量。
> **派生归纳变量**：能写成 `j := c1*i + c2`（i 是基本归纳变量，c1、c2 是循环不变量）的变量。

如果一组派生归纳变量"步调一致"，可以只留一个，其余删除。例如循环里同时算 `t2 = 4*i` 和 `t6 = 4*i`，两者相等 → 只保留一个。

**完整循环优化流程**（按这个顺序做考试不会乱）：
1. 找出循环（识别回边、构造自然循环）
2. 代码外提（把不变运算搬到 preheader）
3. 强度削弱（找归纳变量，用加法替乘法）
4. 删除多余归纳变量、调整循环测试条件

### 10.3.3 数据流分析（理解性内容，不一定考算法细节）

数据流分析就是为做更精确的优化收集信息。**三大经典数据流问题**：

| 问题 | 方向 | 交汇运算 | 典型用途 |
|------|------|---------|---------|
| **到达-定值 (Reaching Definition)** | 前向 | ∪ | 常量传播、复制传播 |
| **活跃变量 (Live Variable)** | 后向 | ∪ | 死代码删除、寄存器分配 |
| **可用表达式 (Available Expression)** | 前向 | ∩ | 全局公共子表达式消除 |

每类问题都有一对方程：
- 块内：`OUT[B] = genB ∪ (IN[B] − killB)`（前向）或对偶（后向）
- 块间：`IN[B] = ⋃ (或 ⋂) {OUT[P] | P 是 B 的前驱}`

迭代求解到不动点为止（每个 OUT 单调增长且有上界，必定收敛）。

> 这部分老师列在了 10.1 概述里，复习时知道有这三类、各自方向和交汇运算即可，**不一定要详细做迭代算例**。

---

# 期末高分复习节奏建议（按 7 天计）

| Day | 内容 | 产出 |
|-----|------|------|
| Day 1 | 第 1 章 + 第 2 章 2.3：流水线 + 文法基本概念 | 默写流水线 6 阶段；能区分推导/句型/句子 |
| Day 2 | 第 3 章：正规式 ↔ NFA ↔ DFA ↔ 最小化 | 手算 `(a\|b)*abb` 全流程 |
| Day 3 | 第 4 章 4.1–4.3：左递归消除、FIRST/FOLLOW、LL(1) | 默写文法 4.2 的 FIRST、FOLLOW、预测表 |
| Day 4 | 第 4 章 4.4–4.5：递归下降、预测分析总控算法 | 手工跟踪 `i+i*i#` 的栈/输入/动作表 |
| Day 5 | 第 5 章：移进-归约、算符优先 | 手算例 5.4 的 FIRSTVT/LASTVT/优先表 |
| Day 6 | 第 7 章 7.1：4 种中间形式 | 把 `a := b * −c + b * −c` 写成四种形式 |
| Day 7 | 第 10 章：基本块划分 + DAG + 循环优化 | 给一段代码能切基本块、画 DAG、识别循环 |

---

# 期末必背公式 / 流程清单（考前一晚翻这页就够）

1. **编译流水线 6 阶段**：词法→语法→语义→中间代码→优化→目标代码（+ 符号表 + 错误处理）
2. **CFG 四元组**：(Vₙ, Vₜ, P, S)
3. **二义性消除**：分层（按优先级）
4. **正规式 → NFA**：Thompson 构造 6 条规则（ε、a、|、·、\*）
5. **NFA → DFA**：子集构造（ε-closure + move）
6. **DFA 最小化**：等价划分迭代
7. **消除直接左递归**：`A → Aα | β` → `A → βA', A' → αA' | ε`
8. **提左因子**：`A → αβ₁ | αβ₂` → `A → αA', A' → β₁ | β₂`
9. **FIRST**：能推出串的首符号 + 是否能推 ε
10. **FOLLOW**：紧跟非终结符之后的终结符；`#` 在 FOLLOW(S) 中
11. **LL(1) 判定**：FIRST(α)∩FIRST(β)=∅；ε∈FIRST(β) 时 FIRST(α)∩FOLLOW(A)=∅
12. **预测分析表填表**：对 A→α，把 A→α 填到 M[A, a] (∀a∈FIRST(α))；若 ε∈FIRST(α)，再填 M[A, b] (∀b∈FOLLOW(A))
13. **预测分析总控**：栈顶 = 输入则匹配 / 弹栈；栈顶非终结符则查表反序压栈
14. **句柄**：栈顶上"该被归约"的那一段
15. **FIRSTVT / LASTVT**：首末终结符
16. **算符优先关系 3 条规则**：
    - 终终相邻 → `=·`
    - 终非邻 → `终 <· FIRSTVT(非)`
    - 非终邻 → `LASTVT(非) ·> 终`
    - 终非终（中间夹一个非终结符）→ `=·`
17. **算符优先分析总控**：栈顶终结符 与 输入比关系；`<·` 或 `=·` 移进；`·>` 归约最左素短语
18. **中间代码 4 种**：后缀式 / 三元式 / 间接三元式 / 四元式
19. **四元式格式**：`(op, arg1, arg2, result)`
20. **基本块划分**：找入口语句（第一条 + 跳转目标 + 跳转之后）
21. **DAG**：节点 = 一次运算；同样的 (op, 左, 右) 复用节点；标签 = 持有此值的变量名集合
22. **三大循环优化**：代码外提 + 强度削弱 + 归纳变量删除
23. **必经结点 + 回边 + 自然循环**：识别循环的标准三步

---

# 与三次实验的对照（你已经动手实现过的部分，复习时事半功倍）

| 实验 | 对应章节 | 实战收获 |
|------|---------|---------|
| 实验一 词法分析器 | 第 3 章 3.1 / 3.2 | 状态转换图代码化、单词分类、最长匹配、向前预读 |
| 实验二 预测分析程序 | 第 4 章 4.5 | 栈式总控算法、查表驱动、产生式反序压栈 |
| 实验三 算符优先分析表 | 第 5 章 5.2 | FIRSTVT/LASTVT 计算、三条规则填表、最左素短语归约 |

考前一定**回头看看你实验的源码**（`lexer.py`、`predictor.py`、`op_precedence.py`），每条注释都对应一个知识点，等于在"用代码读教材"。

---

# 容易混淆的概念对子（考前自检）

| 概念 A | 概念 B | 区别 |
|--------|--------|------|
| 编译 | 解释 | 编译产生独立可执行的目标程序；解释一边翻译一边执行 |
| 推导 | 归约 | 推导从 S 向下走（自顶向下用）；归约从 w 向上走（自底向上用） |
| 句型 | 句子 | 句型可含非终结符；句子全是终结符 |
| 最左推导 | 最右推导 | 看每步替换的是最左还是最右非终结符 |
| FIRST | FIRSTVT | FIRST 看任意首符号（含非终结符推出的）；FIRSTVT 只看首**终结符** |
| NFA | DFA | NFA 允许 ε 边和多重出边；DFA 一对一确定 |
| LL(1) | LR(1) | LL 自顶向下、最左推导；LR 自底向上、反向最右推导 |
| 三元式 | 四元式 | 三元式靠位置编号引用；四元式有显式 result 临时变量 |
| 句柄 | 短语 | 句柄是"该被归约的最左短语"；短语是更一般的概念 |
| 自然循环 | 回边 | 回边是一条边；自然循环是回边定义的节点集合 |

---

# 最后两句话

1. **理论 + 实验联动学**：你已经写过实验一/二/三，意味着第 3、4、5 章已经在脑子里跑过一遍代码了——理论复习时**对照实验源码反推为什么这样写**，效率最高。
2. **考前必动手算一遍的三道经典题**：
   - `(a|b)*abb` 的 NFA → DFA → 最小化（覆盖第 3 章）
   - 文法 4.2 的 FIRST / FOLLOW / 预测分析表 / 分析 `i+i*i#`（覆盖第 4 章）
   - 文法例 5.4 的 FIRSTVT / LASTVT / 算符优先表（覆盖第 5 章）

   这三道题占了所有期末"大题"的至少 60%，吃透即可稳过 80 分。剩下的章节看选择/简答题，按上面"必背清单"扫一遍即可。

加油！🎯
